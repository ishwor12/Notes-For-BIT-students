#include<stdio.h>

int main ()
{
    int a, b, c;
    int avg
    printf ("Enter three numbers:a,b,c");
    scanf("%d %d %d", &a, &b, &c);
    float avg = (a+b+c) / 3.0;
    printf(" Average = %f",avg);
    return 0;
}
